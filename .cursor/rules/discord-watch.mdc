---
description: Discord link watch and allowlist pipeline
globs:
  - extension/domains/discord/**
  - extension/core/lib/process-links.ts
  - extension/core/lib/links.ts
  - extension/core/lib/retailer-url.ts
---

# Discord watch

- DOM selectors live **only** in `extension/domains/discord/content/selectors.ts` — bump `SELECTOR_VERSION` after manual verification.
- Bootstrap quiet period (`MESSAGE_BOOTSTRAP_QUIET_MS`) prevents historical links opening on page load.
- Empty allowlist = observe only; `process-links` no-ops.
- Own messages are skipped (`isOwnMessage` in extract/session).
- Thread URLs share parent channel allowlist (`parseChannelId` uses parent segment).
- Prefer visible message `textContent` URLs over Discord redirect `href`s.
- Side panel Discord tab: global Target keywords + SKUs and Walmart keywords are always editable when the extension is enabled; domains stay per-channel via `useChannelDiscordSettings` (400ms debounce). Global watch lists persist via `useGlobalDiscordWatchSettings`.
- Global **watch_keywords** (`target` / `walmart`) gate auto-open per retailer URL when SKU open mode is off; skipped links use history kind `keyword_skipped`.
- Global **SKU open mode**: Target and Walmart via `decideSkuOpenAction` + global `watch_skus.{target,walmart}` (constructed PDPs; independent `sku_skipped` per retailer on miss); other allowlisted domains blocked in SKU mode.
- When global **Auto ATC** (`retailer_auto_atc_enabled`) is enabled, Target product links open via `openTargetLinkRepeated` in core `open-product-link.ts` (repeat count from global `retailer_link_open_count`, default 1; Auto ATC path handled internally); other allowlisted links open via `openPassiveProductLink` in a new window or background tab per global `open_links_in_window` setting (default on).

See `extension/domains/discord/AGENTS.md`.
